# AI replacement suggestions via a local LLM (Ollama)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
  labels: { app.kubernetes.io/name: ai-service, app.kubernetes.io/part-of: shift-happens }
spec:
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: ai-service }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ai-service, app.kubernetes.io/part-of: shift-happens }
    spec:
      containers:
        - name: ai-service
          image: ghcr.io/microservices-happens/ai-service:0.1.0
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: shift-happens-config }
            - secretRef: { name: ai-service-secrets, optional: true }
          env:
            - name: SPRING_APPLICATION_NAME
              value: ai-service
          readinessProbe: { httpGet: { path: /health/readiness, port: 8080 }, periodSeconds: 5 }
          livenessProbe: { httpGet: { path: /health/liveness, port: 8080 }, initialDelaySeconds: 30, periodSeconds: 10 }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: ai-service
spec:
  selector: { app.kubernetes.io/name: ai-service }
  ports: [{ port: 8080, targetPort: 8080 }]
