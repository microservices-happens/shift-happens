# Leave requests, approvals, immutable ledger, leave-approval saga (CQRS) — query role
apiVersion: apps/v1
kind: Deployment
metadata:
  name: leave-query
  labels: { app.kubernetes.io/name: leave-query, app.kubernetes.io/part-of: shift-happens }
spec:
  replicas: 1
  selector:
    matchLabels: { app.kubernetes.io/name: leave-query }
  template:
    metadata:
      labels: { app.kubernetes.io/name: leave-query, app.kubernetes.io/part-of: shift-happens }
    spec:
      containers:
        - name: leave-query
          image: ghcr.io/microservices-happens/leave-service:0.1.0
          ports: [{ containerPort: 8080 }]
          envFrom:
            - configMapRef: { name: shift-happens-config }
            - secretRef: { name: leave-query-secrets, optional: true }
          env:
            - name: SPRING_APPLICATION_NAME
              value: leave-query
            - name: APP_ROLE
              value: query
          readinessProbe: { httpGet: { path: /health/readiness, port: 8080 }, periodSeconds: 5 }
          livenessProbe: { httpGet: { path: /health/liveness, port: 8080 }, initialDelaySeconds: 30, periodSeconds: 10 }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: leave-query
spec:
  selector: { app.kubernetes.io/name: leave-query }
  ports: [{ port: 8080, targetPort: 8080 }]
